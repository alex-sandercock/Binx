[package]
name = "binx-kinship"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Kinship computation utilities for binx"

[dependencies]
anyhow = "1"
binx-types = { path = "../binx-types" }
csv = "1.4"
ndarray = "0.15"

[dev-dependencies]
tempfile = "3"
